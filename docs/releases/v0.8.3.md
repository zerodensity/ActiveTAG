# ActiveTAG Configurator v0.8.3

- Fixes stable-name migration when an identical `ActiveTAG-Configurator.exe` already exists or is running.
- Reuses the verified existing executable instead of attempting to back it up and replace it.
- Preserves the automatic cleanup of the old versioned executable.
