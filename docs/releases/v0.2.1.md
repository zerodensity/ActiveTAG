## New

- Added a custom ActiveTAG application icon based on the chip and wireless-signal design.
- Removed the `RFID` text from the supplied artwork.
- Embedded the icon into the Windows executable for Explorer, the taskbar, and the application window.
- Added nine icon resolutions from 16x16 through 256x256 pixels.

## Versioning

- Added `version.json` as the single application version source.
- Added the version to the application window title.
- Added the version to every release EXE filename.
- Added Windows File Version, Product Version, description, and original filename metadata.
- Added the version to portable ZIP and packaged EXE names.

## Files

- `ActiveTAG-Configurator-v0.2.1.exe`
- `ActiveTAG-Configurator-v0.2.1-Portable-x64.zip`

## Verification

- Native release build completed successfully.
- Native parser tests passed.
- Verified the application title displays `ActiveTAG Configurator v0.2.1`.
- Verified Windows File Version is `0.2.1.0`.
- Verified Windows Product Version is `v0.2.1`.
- Verified the embedded icon can be extracted from the final EXE.
- Verified the ICO contains nine resolution layers.
- Verified the portable ZIP contains the versioned EXE filename.
