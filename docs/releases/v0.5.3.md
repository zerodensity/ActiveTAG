## Interface Fixes

- Added breathing room below the LED Active IDs header so the LED 0 field no
  longer sits directly on the separator line.
- Replaced runtime Win32 message boxes with in-app Dear ImGui modal dialogs for
  write confirmation, success messages, connection errors, and import errors.

## Notes

- The startup fatal error shown when the log file cannot be opened still uses a
  native Windows dialog because the ImGui interface has not been initialized at
  that point.

## Verification

- Passed native parser tests.
- Built successfully with MSVC C++20, Dear ImGui, and DirectX 11.
- Captured and visually checked the v0.5.3 window layout with direct
  `PrintWindow` screenshot verification.
