## Interface

- Replaced the old product tabs with a modern segmented product selector.
- Removed the bevel-style selected tab effect.
- The active product now uses a clean filled accent state with a small bottom
  indicator.
- Renamed the third product area to `Lens Profiling`.
- The selector follows the current Light or Dark theme.

## Verification

- Passed all six CAM profile detection tests.
- Passed all 15 Talent Track profile detection and disabled LED tests.
- Built successfully with MSVC C++20 and the static `/MT` runtime.
