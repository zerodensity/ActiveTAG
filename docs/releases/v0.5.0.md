## Interface

- Rebuilt the desktop UI with Dear ImGui and DirectX 11.
- Kept the app as a native standalone Windows executable.
- Replaced Win32 form controls with a modern tool-style interface.
- Preserved CAM, Talent Track, and Lens Profiling product selection.
- Preserved profile locking, Custom editing, Hex LED IDs, Decimal display, and
  disabled LED handling.

## Internals

- Vendored Dear ImGui `v1.92.8` directly into `third_party/imgui`.
- Kept the existing Active Tag serial protocol, parser, config import/export,
  and write/verify flow.
- Build still uses the static MSVC runtime (`/MT`).
- Portable dependency checks now include the DirectX 11 runtime components used
  by the ImGui renderer.

## Verification

- Passed all six CAM profile detection tests.
- Passed all 15 Talent Track profile detection and disabled LED tests.
- Built successfully with MSVC C++20, Dear ImGui, and DirectX 11.
