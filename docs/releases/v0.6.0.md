## Lens Profiling Profile Update

- Updated Lens Profiling `Profile TAG 1` to Label Group 21 with active LED IDs
  `0x54`, `0xA8`, `0x150`, and `0x2A0`.
- Updated Lens Profiling `Profile TAG 2` to Label Group 22 with active LED IDs
  `0xA02`, `0xA80`, `0x249`, and `0x492`.
- Lens Profiling profile detection now matches Uplink ID / Label Group 21-22.
- The Label Group value is shown as a read-only UI field while Uplink ID remains
  editable.

## Verification

- Added parser coverage for the updated Lens Profiling profile IDs.
- Verified the native parser test suite.
- Built the standalone Windows x64 EXE and portable ZIP package.
