## Interface Fixes

- Reworked the top control card so COM controls, product selection, profile
  selection, and action buttons no longer collide or create an unwanted
  scrollbar.
- Reflowed action buttons into a clean two-row layout.
- Tightened the LED editor spacing so CAM shows LED 0-7 with hex inputs and
  decimal values visible in the default window.
- Fixed the Profile dropdown clipping at the bottom of the top card.
- Added a `Clear Log` button under the Serial Communication Log panel.

## Notes

- `Clear Log` clears the visible in-app log only. The physical
  `ActiveTAG-Configurator.log` file remains append-only.

## Verification

- Passed native parser tests.
- Built successfully with MSVC C++20, Dear ImGui, and DirectX 11.
- Captured and visually checked the v0.5.2 window layout with direct
  `PrintWindow` screenshot verification.
