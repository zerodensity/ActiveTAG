## Interface Fixes

- Replaced the shared editor separator with per-column section lines so the
  `General Settings` header is no longer crossed by a horizontal line.
- Kept the LED and General Settings fields aligned under their own section
  headers with clean spacing.

## Verification

- Passed native parser tests.
- Built successfully with MSVC C++20, Dear ImGui, and DirectX 11.
- Captured and visually checked the v0.5.4 window layout with direct
  `PrintWindow` screenshot verification.
