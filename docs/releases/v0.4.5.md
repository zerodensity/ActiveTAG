## Interface

- Replaced the previous owner-drawn button segments with a single custom
  Win32 product selector control.
- Removed the separate button edges and odd bottom artifacts from the product
  selector.
- The selected product is now drawn as one clean pill inside a unified track.
- The selector keeps the same CAM, Talent Track, and Lens Profiling behavior.

## Verification

- Passed all six CAM profile detection tests.
- Passed all 15 Talent Track profile detection and disabled LED tests.
- Built successfully with MSVC C++20 and the static `/MT` runtime.
